```json
{
  "files": [
    {
      "path": "types/weather.ts",
      "content": "export interface Coordinates {\n  lat: number;\n  lon: number;\n}\n\nexport interface WeatherCondition {\n  id: number;\n  main: string;\n  description: string;\n  icon: string;\n}\n\nexport interface CurrentWeather {\n  city: string;\n  country: string;\n  coordinates: Coordinates;\n  timezone: string;\n  timestamp: number;\n  temperature: number;\n  feelsLike: number;\n  tempMin: number;\n  tempMax: number;\n  humidity: number;\n  pressure: number;\n  seaLevelPressure: number;\n  groundLevelPressure: number;\n  visibility: number;\n  dewPoint: number;\n  cloudCover: number;\n  condition: WeatherCondition;\n  wind: WindData;\n  precipitation: PrecipitationData;\n  sun: SolarData;\n  uv: UVData;\n  airQuality: AirQuality;\n}\n\nexport interface WindData {\n  speed: number;\n  gust: number;\n  direction: number;\n  bearing: string;\n  chill: number;\n}\n\nexport interface PrecipitationData {\n  type: 'none' | 'rain' | 'snow' | 'hail' | 'sleet';\n  intensity: number;\n  probability: number;\n  radarReflectivity: number;\n}\n\nexport interface SolarData {\n  sunrise: number;\n  sunset: number;\n  civilTwilightStart: number;\n  civilTwilightEnd: number;\n  nauticalTwilightStart: number;\n  nauticalTwilightEnd: number;\n  astronomicalTwilightStart: number;\n  astronomicalTwilightEnd: number;\n  solarRadiation: number;\n  dayLength: number;\n}\n\nexport interface UVData {\n  index: number;\n  maxIndex: number;\n  maxTime: number;\n  category: 'low' | 'moderate' | 'high' | 'very_high' | 'extreme';\n  burnTime: number;\n}\n\nexport interface AirQuality {\n  aqi: number;\n  category: 'good' | 'moderate' | 'unhealthy_sensitive' | 'unhealthy' | 'very_unhealthy' | 'hazardous';\n  dominantPollutant: string;\n  components: {\n    pm25: number;\n    pm10: number;\n    o3: number;\n    no2: number;\n    co: number;\n    so2: number;\n    nh3: number;\n  };\n  healthRecommendation: string;\n}\n\nexport interface HourlyForecast {\n  timestamp: number;\n  temperature: number;\n  feelsLike: number;\n  humidity: number;\n  pressure: number;\n  dewPoint: number;\n  cloudCover: number;\n  visibility: number;\n  condition: WeatherCondition;\n  wind: WindData;\n  precipitation: PrecipitationData;\n  uv: UVData;\n  pop: number;\n}\n\nexport interface DailyForecast {\n  timestamp: number;\n  date: string;\n  tempMin: number;\n  tempMax: number;\n  tempMorn: number;\n  tempDay: number;\n  tempEve: number;\n  tempNight: number;\n  feelsLikeDay: number;\n  feelsLikeNight: number;\n  humidity: number;\n  pressure: number;\n  dewPoint: number;\n  cloudCover: number;\n  condition: WeatherCondition;\n  wind: WindData;\n  precipitation: PrecipitationData;\n  uv: UVData;\n  pop: number;\n  moonPhase: number;\n  sunrise: number;\n  sunset: number;\n}\n\nexport interface WeatherAlert {\n  id: string;\n  event: string;\n  headline: string;\n  description: string;\n  severity: 'minor' | 'moderate' | 'severe' | 'extreme';\n  certainty: 'possible' | 'likely' | 'observed';\n  urgency: 'future' | 'expected' | 'immediate';\n  senderName: string;\n  start: number;\n  end: number;\n  areas: string[];\n  tags: string[];\n}\n\nexport interface ContextualMetrics {\n  clothingIndex: number;\n  clothingRecommendation: string;\n  activityScore: number;\n  activityRecommendation: string;\n  healthRisks: HealthRisks;\n  commuteImpact: CommuteImpact;\n}\n\nexport interface HealthRisks {\n  asthmaRisk: number;\n  migraineIndex: number;\n  arthritisDiscomfort: number;\n  overallRisk: 'low' | 'moderate' | 'high';\n}\n\nexport interface CommuteImpact {\n  roadConditions: 'clear' | 'wet' | 'icy' | 'foggy' | 'hazardous';\n  delayMinutes: number;\n  airportDisruptionIndex: number;\n}\n\nexport interface WeatherData {\n  current: CurrentWeather;\n  hourly: HourlyForecast[];\n  daily: DailyForecast[];\n  alerts: WeatherAlert[];\n  contextual: ContextualMetrics;\n}\n\nexport interface SearchResult {\n  city: string;\n  country: string;\n  state?: string;\n  coordinates: Coordinates;\n}\n"
    },
    {
      "path": "lib/weatherService.ts",
      "content": "import type {\n  WeatherData,\n  CurrentWeather,\n  HourlyForecast,\n  DailyForecast,\n  WeatherAlert,\n  ContextualMetrics,\n  WindData,\n  PrecipitationData,\n  SolarData,\n  UVData,\n  AirQuality,\n  WeatherCondition,\n} from '@/types/weather';\n\nconst CITY_PROFILES: Record<\n  string,\n  {\n    country: string;\n    timezone: string;\n    lat: number;\n    lon: number;\n    tempBase: number;\n    humidityBase: number;\n    windBase: number;\n    climate: 'tropical' | 'temperate' | 'arid' | 'continental' | 'polar';\n  }\n> = {\n  'New York': {\n    country: 'US',\n    timezone: 'America/New_York',\n    lat: 40.7128,\n    lon: -74.006,\n    tempBase: 18,\n    humidityBase: 62,\n    windBase: 14,\n    climate: 'temperate',\n  },\n  London: {\n    country: 'GB',\n    timezone: 'Europe/London',\n    lat: 51.5074,\n    lon: -0.1278,\n    tempBase: 13,\n    humidityBase: 75,\n    windBase: 18,\n    climate: 'temperate',\n  },\n  Tokyo: {\n    country: 'JP',\n    timezone: 'Asia/Tokyo',\n    lat: 35.6762,\n    lon: 139.6503,\n    tempBase: 17,\n    humidityBase: 68,\n    windBase: 11,\n    climate: 'temperate',\n  },\n  Sydney: {\n    country: 'AU',\n    timezone: 'Australia/Sydney',\n    lat: -33.8688,\n    lon: 151.2093,\n    tempBase: 22,\n    humidityBase: 58,\n    windBase: 16,\n    climate: 'temperate',\n  },\n  Dubai: {\n    country: 'AE',\n    timezone: 'Asia/Dubai',\n    lat: 25.2048,\n    lon: 55.2708,\n    tempBase: 36,\n    humidityBase: 48,\n    windBase: 13,\n    climate: 'arid',\n  },\n  Paris: {\n    country: 'FR',\n    timezone: 'Europe/Paris',\n    lat: 48.8566,\n    lon: 2.3522,\n    tempBase: 15,\n    humidityBase: 71,\n    windBase: 12,\n    climate: 'temperate',\n  },\n  Singapore: {\n    country: 'SG',\n    timezone: 'Asia/Singapore',\n    lat: 1.3521,\n    lon: 103.8198,\n    tempBase: 30,\n    humidityBase: 84,\n    windBase: 8,\n    climate: 'tropical',\n  },\n  Moscow: {\n    country: 'RU',\n    timezone: 'Europe/Moscow',\n    lat: 55.7558,\n    lon: 37.6173,\n    tempBase: 4,\n    humidityBase: 78,\n    windBase: 15,\n    climate: 'continental',\n  },\n  'Los Angeles': {\n    country: 'US',\n    timezone: 'America/Los_Angeles',\n    lat: 34.0522,\n    lon: -118.2437,\n    tempBase: 24,\n    humidityBase: 55,\n    windBase: 9,\n    climate: 'arid',\n  },\n  Mumbai: {\n    country: 'IN',\n    timezone: 'Asia/Kolkata',\n    lat: 19.076,\n    lon: 72.8777,\n    tempBase: 31,\n    humidityBase: 79,\n    windBase: 17,\n    climate: 'tropical',\n  },\n};\n\nconst DEFAULT_PROFILE = {\n  country: 'US',\n  timezone: 'America/New_York',\n  lat: 40.7128,\n  lon: -74.006,\n  tempBase: 20,\n  humidityBase: 60,\n  windBase: 12,\n  climate: 'temperate' as const,\n};\n\nconst CONDITIONS: WeatherCondition[] = [\n  { id: 800, main: 'Clear', description: 'clear sky', icon: 'clear-day' },\n  { id: 801, main: 'Clouds', description: 'few clouds', icon: 'partly-cloudy-day' },\n  { id: 802, main: 'Clouds', description: 'scattered clouds', icon: 'partly-cloudy-day' },\n  { id: 803, main: 'Clouds', description: 'broken clouds', icon: 'cloudy' },\n  { id: 804, main: 'Clouds', description: 'overcast clouds', icon: 'overcast' },\n  { id: 500, main: 'Rain', description: 'light rain', icon: 'rain' },\n  { id: 501, main: 'Rain', description: 'moderate rain', icon: 'rain' },\n  { id: 502, main: 'Rain', description: 'heavy rain', icon: 'heavy-rain' },\n  { id: 600, main: 'Snow', description: 'light snow', icon: 'snow' },\n  { id: 601, main: 'Snow', description: 'snow', icon: 'snow' },\n  { id: 611, main: 'Snow', description: 'sleet', icon: 'sleet' },\n  { id: 200, main: 'Thunderstorm', description: 'thunderstorm with light rain', icon: 'thunderstorm' },\n  { id: 741, main: 'Fog', description: 'fog', icon: 'fog' },\n  { id: 721, main: 'Haze', description: 'haze', icon: 'haze' },\n  { id: 771, main: 'Squall', description: 'squalls', icon: 'wind' },\n];\n\nfunction seededRandom(seed: number): () => number {\n  let s = seed;\n  return function () {\n    s = (s * 9301 + 49297) % 233280;\n    return s / 233280;\n  };\n}\n\nfunction cityHash(city: string): number {\n  let hash = 0;\n  for (let i = 0; i < city.length; i++) {\n    hash = (hash << 5) - hash + city.charCodeAt(i);\n    hash |= 0;\n  }\n  return Math.abs(hash);\n}\n\nfunction generateWindData(rng: () => number, baseSpeed: number): WindData {\n  const speed = Math.round((baseSpeed + (rng() - 0.5) * 10) * 10) / 10;\n  const gust = Math.round((speed * (1.2 + rng() * 0.8)) * 10) / 10;\n  const direction = Math.round(rng() * 360);\n  const bearings = ['N', 'NNE', 'NE', 'ENE', 'E', 'ESE', 'SE', 'SSE', 'S', 'SSW', 'SW', 'WSW', 'W', 'WNW', 'NW', 'NNW'];\n  const bearing = bearings[Math.round(direction / 22.5) % 16];\n  const chill = Math.round(13.12 + 0.6215 * 20 - 11.37 * Math.pow(speed, 0.16) + 0.3965 * 20 * Math.pow(speed, 0.16));\n  return { speed, gust, direction, bearing, chill };\n}\n\nfunction generatePrecipitation(rng: () => number, temp: number, climate: string): PrecipitationData {\n  const rainyChance = climate === 'tropical' ? 0.6 : climate === 'arid' ? 0.1 : 0.35;\n  const isRaining = rng() < rainyChance;\n  if (!isRaining) {\n    return { type: 'none', intensity: 0, probability: Math.round(rng() * 20), radarReflectivity: 0 };\n  }\n  const type = temp <= 0 ? 'snow' : temp <= 3 ? 'sleet' : 'rain';\n  const intensity = Math.round((rng() * 5 + 0.5) * 10) / 10;\n  const probability = Math.round(40 + rng() * 55);\n  const radarReflectivity = Math.round(intensity * 8 + rng() * 15);\n  return { type, intensity, probability, radarReflectivity };\n}\n\nfunction generateSolarData(rng: () => number, now: number, lat: number): SolarData {\n  const dayMs = 86400 * 1000;\n  const midday = Math.floor(now / dayMs) * dayMs + 12 * 3600 * 1000;\n  const dayLength = (7 + rng() * 6) * 3600;\n  const sunrise = Math.round((midday - dayLength / 2) / 1000);\n  const sunset = Math.round((midday + dayLength / 2) / 1000);\n  return {\n    sunrise,\n    sunset,\n    civilTwilightStart: sunrise - 1800,\n    civilTwilightEnd: sunset + 1800,\n    nauticalTwilightStart: sunrise - 3600,\n    nauticalTwilightEnd: sunset + 3600,\n    astronomicalTwilightStart: sunrise - 5400,\n    astronomicalTwilightEnd: sunset + 5400,\n    solarRadiation: Math.round(300 + rng() * 700),\n    dayLength: Math.round(dayLength),\n  };\n}\n\nfunction generateUVData(rng: () => number, condition: WeatherCondition): UVData {\n  const isCloudy = ['Clouds', 'Rain', 'Thunderstorm', 'Fog', 'Snow'].includes(condition.main);\n  const maxIdx = isCloudy\n    ? Math.round(rng() * 3 * 10) / 10\n    : Math.round((4 + rng() * 7) * 10) / 10;\n  const index = Math.round(maxIdx * (0.6 + rng() * 0.4) * 10) / 10;\n  let category: UVData['category'] = 'low';\n  if (index >= 11) category = 'extreme';\n  else if (index >= 8) category = 'very_high';\n  else if (index >= 6) category = 'high';\n  else if (index >= 3) category = 'moderate';\n  const burnTimes = { low: 60, moderate: 30, high: 15, very_high: 10, extreme: 5 };\n  return {\n    index,\n    maxIndex: maxIdx,\n    maxTime: Date.now() / 1000 + 2 * 3600,\n    category,\n    burnTime: burnTimes[category],\n  };\n}\n\nfunction generateAirQuality(rng: () => number, climate: string): AirQuality {\n  const baseAQI = climate === 'tropical' ? 40 : climate === 'arid' ? 80 : 35;\n  const aqi = Math.round(baseAQI + (rng() - 0.3) * 80);\n  const clampedAQI = Math.max(5, Math.min(300, aqi));\n  let category: AirQuality['category'] = 'good';\n  if (clampedAQI > 200) category = 'very_unhealthy';\n  else if (clampedAQI > 150) category = 'unhealthy';\n  else if (clampedAQI > 100) category = 'unhealthy_sensitive';\n  else if (clampedAQI > 50) category = 'moderate';\n  const recommendations: Record<AirQuality['category'], string> = {\n    good: 'Air quality is satisfactory. No health concerns.',\n    moderate: 'Acceptable air quality. Sensitive individuals should limit prolonged outdoor exertion.',\n    unhealthy_sensitive: 'People with respiratory or heart disease, elderly and children should limit prolonged outdoor exertion.',\n    unhealthy: 'Everyone may experience health effects. Limit prolonged outdoor exertion.',\n    very_unhealthy: 'Health alert. Everyone should avoid outdoor activities.',\n    hazardous: 'Health emergency. Everyone should remain indoors.',\n  };\n  return {\n    aqi: clampedAQI,\n    category,\n    dominantPollutant: rng() > 0.5 ? 'pm2_5' : 'o3',\n    components: {\n      pm25: Math.round((5 + rng() * 40) * 10) / 10,\n      pm10: Math.round((10 + rng() * 60) * 10) / 10,\n      o3: Math.round((20 + rng() * 80) * 10) / 10,\n      no2: Math.round((5 + rng() * 50) * 10) / 10,\n      co: Math.round((200 + rng() * 1000) * 10) / 10,\n      so2: Math.round((1 + rng() * 20) * 10) / 10,\n      nh3: Math.round((0.5 + rng() * 10) * 10) / 10,\n    },\n    healthRecommendation: recommendations[category],\n  };\n}\n\nfunction generateCurrentWeather(\n  city: string,\n  profile: typeof DEFAULT_PROFILE,\n  rng: () => number,\n  now: number\n): CurrentWeather {\n  const temp = Math.round((profile.tempBase + (rng() - 0.5) * 14) * 10) / 10;\n  const humidity = Math.round(Math.max(10, Math.min(99, profile.humidityBase + (rng() - 0.5) * 30)));\n  const condition = CONDITIONS[Math.floor(rng() * CONDITIONS.length)];\n  const wind = generateWindData(rng, profile.windBase);\n  const precipitation = generatePrecipitation(rng, temp, profile.climate);\n  const sun = generateSolarData(rng, now, profile.lat);\n  const uv = generateUVData(rng, condition);\n  const airQuality = generateAirQuality(rng, profile.climate);\n  return {\n    city,\n    country: profile.country,\n    coordinates: { lat: profile.lat, lon: profile.lon },\n    timezone: profile.timezone,\n    timestamp: Math.round(now / 1000),\n    temperature: temp,\n    feelsLike: Math.round((temp - (wind.speed > 10 ? 2 : 0) - (humidity > 70 ? 1 : 0)) * 10) / 10,\n    tempMin: Math.round((temp - 3 - rng() * 4) * 10) / 10,\n    tempMax: Math.round((temp + 2 + rng() * 5) * 10) / 10,\n    humidity,\n    pressure: Math.round(1000 + (rng() - 0.5) * 40),\n    seaLevelPressure: Math.round(1013 + (rng() - 0.5) * 30),\n    groundLevelPressure: Math.round(1005 + (rng() - 0.5) * 30),\n    visibility: Math.round((5 + rng() * 15) * 10) / 10,\n    dewPoint: Math.round((temp - (100 - humidity) / 5) * 10) / 10,\n    cloudCover: Math.round(rng() * 100),\n    condition,\n    wind,\n    precipitation,\n    sun,\n    uv,\n    airQuality,\n  };\n}\n\nfunction generateHourlyForecast(\n  baseTemp: number,\n  profile: typeof DEFAULT_PROFILE,\n  rng: () => number,\n  startTime: number\n): HourlyForecast[] {\n  const hours: HourlyForecast[] = [];\n  for (let i = 0; i < 48; i++) {\n    const timestamp = startTime + i * 3600;\n    const hourOfDay = new Date(timestamp * 1000).getHours();\n    const tempVariance = Math.sin(((hourOfDay - 6) * Math.PI) / 12) * 5;\n    const temp = Math.round((baseTemp + tempVariance + (rng() - 0.5) * 3) * 10) / 10;\n    const humidity = Math.round(Math.max(10, Math.min(99, profile.humidityBase + (rng() - 0.5) * 20)));\n    const condition = CONDITIONS[Math.floor(rng() * CONDITIONS.length)];\n    hours.push({\n      timestamp,\n      temperature: temp,\n      feelsLike: Math.round((temp - rng() * 2) * 10) / 10,\n      humidity,\n      pressure: Math.round(1010 + (rng() - 0.5) * 20),\n      dewPoint: Math.round((temp - (100 - humidity) / 5) * 10) / 10,\n      cloudCover: Math.round(rng() * 100),\n      visibility: Math.round((5 + rng() * 15) * 10) / 10,\n      condition,\n      wind: generateWindData(rng, profile.windBase),\n      precipitation: generatePrecipitation(rng, temp, profile.climate),\n      uv: generateUVData(rng, condition),\n      pop: Math.round(rng() * 100),\n    });\n  }\n  return hours;\n}\n\nfunction generateDailyForecast(\n  baseTemp: number,\n  profile: typeof DEFAULT_PROFILE,\n  rng: () => number,\n  startTime: number\n): DailyForecast[] {\n  const days: DailyForecast[] = [];\n  for (let i = 0; i < 14; i++) {\n    const timestamp = startTime + i * 86400;\n    const date = new Date(timestamp * 1000);\n    const tempMin = Math.round((baseTemp - 3 - rng() * 5) * 10) / 10;\n    const tempMax = Math.round((baseTemp + 3 + rng() * 6) * 10) / 10;\n    const condition = CONDITIONS[Math.floor(rng() * CONDITIONS.length)];\n    const sun = generateSolarData(rng, timestamp * 1000, profile.lat);\n    days.push({\n      timestamp,\n      date: date.toISOString().split('T')[0],\n      tempMin,\n      tempMax,\n      tempMorn: Math.round((tempMin + rng() * 3) * 10) / 10,\n      tempDay: Math.round(((tempMin + tempMax) / 2 + rng() * 2) * 10) / 10,\n      tempEve: Math.round((tempMax - rng() * 4) * 10) / 10,\n      tempNight: Math.round((tempMin + rng() * 2) * 10) / 10,\n      feelsLikeDay: Math.round(((tempMin + tempMax) / 2 - rng() * 3) * 10) / 10,\n      feelsLikeNight: Math.round((tempMin - rng() * 4) * 10) / 10,\n      humidity: Math.round(Math.max(10, Math.min(99, profile.humidityBase + (rng() - 0.5) * 25))),\n      pressure: Math.round(1010 + (rng() - 0.5) * 25),\n      dewPoint: Math.round((baseTemp - (100 - profile.humidityBase) / 5 + (rng() - 0.5) * 3) * 10) / 10,\n      cloudCover: Math.round(rng() * 100),\n      condition,\n      wind: generateWindData(rng, profile.windBase),\n      precipitation: generatePrecipitation(rng, baseTemp, profile.climate),\n      uv: generateUVData(rng, condition),\n      pop: Math.round(rng() * 100),\n      moonPhase: Math.round(rng() * 100) / 100,\n      sunrise: sun.sunrise,\n      sunset: sun.sunset,\n    });\n  }\n  return days;\n}\n\nfunction generateAlerts(rng: () => number): WeatherAlert[] {\n  if (rng() > 0.3) return [];\n  const alertTypes = [\n    { event: 'Winter Storm Warning', headline: 'Winter Storm Warning in Effect', severity: 'severe' as const, tags: ['snow', 'ice', 'wind'] },\n    { event: 'Severe Thunderstorm Watch', headline: 'Severe Thunderstorm Watch Until 9 PM', severity: 'moderate' as const, tags: ['thunder', 'lightning'] },\n    { event: 'Heat Advisory', headline: 'Heat Advisory: Temperatures Reaching 38C', severity: 'moderate' as const, tags: ['heat', 'humidity'] },\n    { event: 'Dense Fog Advisory', headline: 'Dense Fog Advisory Through Midday', severity: 'minor' as const, tags: ['fog', 'visibility'] },\n    { event: 'Flood Watch', headline: 'Flood Watch Until Thursday Evening', severity: 'severe' as const, tags: ['flood', 'rain'] },\n  ];\n  const alert = alertTypes[Math.floor(rng() * alertTypes.length)];\n  const now = Date.now() / 1000;\n  return [\n    {\n      id: `alert-${Math.round(rng() * 99999)}`,\n      event: alert.event,\n      headline: alert.headline,\n      description: `The National Weather Service has issued a ${alert.event}. Residents should take precautions and stay informed of changing weather conditions.`,\n      severity: alert.severity,\n      certainty: 'likely',\n      urgency: 'expected',\n      senderName: 'National Weather Service',\n      start: now,\n      end: now + 12 * 3600,\n      areas: ['Metro Area', 'Surrounding Regions'],\n      tags: alert.tags,\n    },\n  ];\n}\n\nfunction generateContextualMetrics(current: CurrentWeather, rng: () => number): ContextualMetrics {\n  const temp = current.temperature;\n  const uv = current.uv.index;\n  const wind = current.wind.speed;\n  const rain = current.precipitation.type !== 'none';\n  let clothingIndex: number;\n  let clothingRec: string;\n  if (temp < 0) { clothingIndex = 1; clothingRec = 'Heavy coat, gloves, scarf, and thermal layers required.'; }\n  else if (temp < 10) { clothingIndex = 3; clothingRec = 'Winter coat and warm layers recommended.'; }\n  else if (temp < 18) { clothingIndex = 5; clothingRec = 'Light jacket or sweater appropriate.'; }\n  else if (temp < 25) { clothingIndex = 7; clothingRec = 'Light clothing. A layer for evenings.'; }\n  else { clothingIndex = 9; clothingRec = 'Light, breathable summer clothing. Sun protection advised.'; }\n  const activityScore = Math.round(\n    Math.max(0, Math.min(100,\n      80 - (Math.abs(temp - 22) * 2) - (rain ? 30 : 0) - (wind > 30 ? 20 : 0) - (uv > 8 ? 15 : 0)\n    ))\n  );\n  let activityRec = 'Excellent conditions for outdoor activities!';\n  if (activityScore < 30) activityRec = 'Poor conditions. Indoor activities recommended.';\n  else if (activityScore < 55) activityRec = 'Fair conditions. Dress appropriately.';\n  else if (activityScore < 75) activityRec = 'Good conditions for most outdoor activities.';\n  return {\n    clothingIndex,\n    clothingRecommendation: clothingRec,\n    activityScore,\n    activityRecommendation: activityRec,\n    healthRisks: {\n      asthmaRisk: Math.round(20 + (current.airQuality.aqi > 100 ? 40 : 0) + rng() * 30),\n      migraineIndex: Math.round(15 + (Math.abs(current.pressure - 1013) > 15 ? 35 : 0) + rng() * 25),\n      arthritisDiscomfort: Math.round(10 + (temp < 10 ? 30 : 0) + (current.humidity > 75 ? 20 : 0) + rng() * 20),\n      overallRisk: current.airQuality.aqi > 150 ? 'high' : current.airQuality.aqi > 100 ? 'moderate' : 'low',\n    },\n    commuteImpact: {\n      roadConditions: rain ? (temp < 3 ? 'icy' : 'wet') : current.condition.main === 'Fog' ? 'foggy' : 'clear',\n      delayMinutes: rain ? Math.round(5 + rng() * 25) : 0,\n      airportDisruptionIndex: Math.round(rng() * (rain ? 60 : 20)),\n    },\n  };\n}\n\nexport async function getWeatherData(city: string): Promise<WeatherData> {\n  await new Promise((res) => setTimeout(res, 350 + Math.random() * 200));\n  const normalizedCity = Object.keys(CITY_PROFILES).find(\n    (k) => k.toLowerCase() === city.toLowerCase()\n  ) || city;\n  const profile = CITY_PROFILES[normalizedCity] || {\n    ...DEFAULT_PROFILE,\n    tempBase: DEFAULT_PROFILE.tempBase + (cityHash(city) % 20) - 10,\n    humidityBase: DEFAULT_PROFILE.humidityBase + (cityHash(city) % 30) - 15,\n    windBase: DEFAULT_PROFILE.windBase + (cityHash(city) % 10) - 5,\n  };\n  const seed = cityHash(normalizedCity) + Math.floor(Date.now() / (1000 * 60 * 30));\n  const rng = seededRandom(seed);\n  const now = Date.now();\n  const current = generateCurrentWeather(normalizedCity, profile, rng, now);\n  const hourly = generateHourlyForecast(current.temperature, profile, rng, Math.round(now / 1000));\n  const daily = generateDailyForecast(current.temperature, profile, rng, Math.round(now / (1000 * 86400)) * 86400);\n  const alerts = generateAlerts(rng);\n  const contextual = generateContextualMetrics(current, rng);\n  return { current, hourly, daily, alerts, contextual };\n}\n\nexport async function searchCities(query: string): Promise<{ city: string; country: string; display: string }[]> {\n  await new Promise((res) => setTimeout(res, 100 + Math.random() * 100));\n  if (!query || query.length < 2) return [];\n  const allCities = Object.entries(CITY_PROFILES).map(([name, p]) => ({\n    city: name,\n    country: p.country,\n    display: `${name}, ${p.country}`,\n  }));\n  return allCities\n    .filter((c) => c.city.toLowerCase().includes(query.toLowerCase()))\n    .slice(0, 6);\n}\n"
    },
    {
      "path": "lib/weatherUtils.ts",
      "content": "import type { AirQuality, UVData, WeatherCondition } from '@/types/weather';\n\nexport type WeatherIconName =\n  | 'clear-day'\n  | 'clear-night'\n  | 'partly-cloudy-day'\n  | 'partly-cloudy-night'\n  | 'cloudy'\n  | 'overcast'\n  | 'rain'\n  | 'heavy-rain'\n  | 'snow'\n  | 'sleet'\n  | 'thunderstorm'\n  | 'fog'\n  | 'haze'\n  | 'wind'\n  | 'unknown';\n\nexport interface WeatherIconConfig {\n  name: WeatherIconName;\n  label: string;\n  colorClass: string;\n  bgClass: string;\n}\n\nconst CONDITION_ICON_MAP: Record<number, WeatherIconConfig> = {\n  // Thunderstorm group (2xx)\n  200: { name: 'thunderstorm', label: 'Thunderstorm with Light Rain', colorClass: 'text-violet-400', bgClass: 'bg-violet-950/40' },\n  201: { name: 'thunderstorm', label: 'Thunderstorm with Rain', colorClass: 'text-violet-400', bgClass: 'bg-violet-950/40' },\n  202: { name: 'thunderstorm', label: 'Thunderstorm with Heavy Rain', colorClass: 'text-violet-500', bgClass: 'bg-violet-950/50' },\n  210: { name: 'thunderstorm', label: 'Light Thunderstorm', colorClass: 'text-violet-400', bgClass: 'bg-violet-950/40' },\n  211: { name: 'thunderstorm', label: 'Thunderstorm', colorClass: 'text-violet-500', bgClass: 'bg-violet-950/50' },\n  212: { name: 'thunderstorm', label: 'Heavy Thunderstorm', colorClass: 'text-violet-600', bgClass: 'bg-violet-950/60' },\n  221: { name: 'thunderstorm', label: 'Ragged Thunderstorm', colorClass: 'text-violet-600', bgClass: 'bg-violet-950/60' },\n  230: { name: 'thunderstorm', label: 'Thunderstorm with Light Drizzle', colorClass: 'text-violet-400', bgClass: 'bg-violet-950/40' },\n  231: { name: 'thunderstorm', label: 'Thunderstorm with Drizzle', colorClass: 'text-violet-400', bgClass: 'bg-violet-950/40' },\n  232: { name: 'thunderstorm', label: 'Thunderstorm with Heavy Drizzle', colorClass: 'text-violet-500', bgClass: 'bg-violet-950/50' },\n  // Drizzle group (3xx)\n  300: { name: 'rain', label: 'Light Drizzle', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  301: { name: 'rain', label: 'Drizzle', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  302: { name: 'heavy-rain', label: 'Heavy Drizzle', colorClass: 'text-sky-500', bgClass: 'bg-sky-950/40' },\n  310: { name: 'rain', label: 'Light Drizzle Rain', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  311: { name: 'rain', label: 'Drizzle Rain', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  312: { name: 'heavy-rain', label: 'Heavy Drizzle Rain', colorClass: 'text-sky-500', bgClass: 'bg-sky-950/40' },\n  313: { name: 'rain', label: 'Shower Rain and Drizzle', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  314: { name: 'heavy-rain', label: 'Heavy Shower Rain and Drizzle', colorClass: 'text-sky-500', bgClass: 'bg-sky-950/40' },\n  321: { name: 'rain', label: 'Shower Drizzle', colorClass: 'text-sky-400', bgClass: 'bg-sky-950/30' },\n  // Rain group (5xx)\n  500: { name: 'rain', label: 'Light Rain', colorClass: 'text-blue-400', bgClass: 'bg-blue-950/30' },\n  501: { name: 'rain', label: 'Moderate Rain', colorClass: 'text-blue-500', bgClass: 'bg-blue-950/40' },\n  502: { name: 'heavy-rain', label: 'Heavy Rain', colorClass: 'text-blue-600', bgClass: 'bg-blue-950/50' },\n  503: { name: 'heavy-rain', label: 'Very Heavy Rain', colorClass: 'text-blue-700', bgClass: 'bg-blue-950/60' },\n  504: { name: 'heavy-rain', label: 'Extreme Rain', colorClass: 'text-blue-800', bgClass: 'bg-blue-950/70' },\n  511: { name: 'sleet', label: 'Freezing Rain', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  520: { name: 'rain', label: 'Light Shower Rain', colorClass: 'text-blue-400', bgClass: 'bg-blue-950/30' },\n  521: { name: 'rain', label: 'Shower Rain', colorClass: 'text-blue-500', bgClass: 'bg-blue-950/40' },\n  522: { name: 'heavy-rain', label: 'Heavy Shower Rain', colorClass: 'text-blue-600', bgClass: 'bg-blue-950/50' },\n  531: { name: 'rain', label: 'Ragged Shower Rain', colorClass: 'text-blue-500', bgClass: 'bg-blue-950/40' },\n  // Snow group (6xx)\n  600: { name: 'snow', label: 'Light Snow', colorClass: 'text-cyan-300', bgClass: 'bg-cyan-950/20' },\n  601: { name: 'snow', label: 'Snow', colorClass: 'text-cyan-300', bgClass: 'bg-cyan-950/30' },\n  602: { name: 'snow', label: 'Heavy Snow', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  611: { name: 'sleet', label: 'Sleet', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  612: { name: 'sleet', label: 'Light Shower Sleet', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/30' },\n  613: { name: 'sleet', label: 'Shower Sleet', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  615: { name: 'sleet', label: 'Light Rain and Snow', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/30' },\n  616: { name: 'sleet', label: 'Rain and Snow', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  620: { name: 'snow', label: 'Light Shower Snow', colorClass: 'text-cyan-300', bgClass: 'bg-cyan-950/20' },\n  621: { name: 'snow', label: 'Shower Snow', colorClass: 'text-cyan-300', bgClass: 'bg-cyan-950/30' },\n  622: { name: 'snow', label: 'Heavy Shower Snow', colorClass: 'text-cyan-400', bgClass: 'bg-cyan-950/40' },\n  // Atmosphere group (7xx)\n  701: { name: 'fog', label: 'Mist', colorClass: 'text-slate-400', bgClass: 'bg-slate-800/40' },\n  711: { name: 'haze', label: 'Smoke', colorClass: 'text-amber-400', bgClass: 'bg-amber-950/30' },\n  721: { name: 'haze', label: 'Haze', colorClass: 'text-amber-300', bgClass: 'bg-amber-950/20' },\n  731: { name: 'haze', label: 'Sand/Dust Whirls', colorClass: 'text-yellow-400', bgClass: 'bg-yellow-950/30' },\n  741: { name: 'fog', label: 'Fog', colorClass: 'text-slate-300', bgClass: 'bg-slate-800/30' },\n  751: { name: 'haze', label: 'Sand', colorClass: 'text-yellow-400', bgClass: 'bg-yellow-950/30' },\n  761: { name: 'haze', label: 'Dust', colorClass: 'text-yellow-400', bgClass: 'bg-yellow-950/30' },\n  762: { name: 'haze', label: 'Volcanic Ash', colorClass: 'text-gray-500', bgClass: 'bg-gray-900/40' },\n  771: { name: 'wind', label: 'Squalls', colorClass: 'text-teal-400', bgClass: 'bg-teal-950/30' },\n  781: { name: 'thunderstorm', label: 'Tornado', colorClass: 'text-red-500', bgClass: 'bg-red-950/50' },\n  // Clear group (800)\n  800: { name: 'clear-day', label: 'Clear Sky', colorClass: 'text-amber-400', bgClass: 'bg-amber-900/20' },\n  // Clouds group (80x)\n  801: { name: 'partly-cloudy-day', label: 'Few Clouds', colorClass: 'text-slate-300', bgClass: 'bg-slate-800/20' },\n  802: { name: 'partly-cloudy-day', label: 'Scattered Clouds', colorClass: 'text-slate-300', bgClass: 'bg-slate-800/30' },\n  803: { name: 'cloudy', label: 'Broken Clouds', colorClass: 'text-slate-400', bgClass: 'bg-slate-800/40' },\n  804: { name: 'overcast', label: 'Overcast Clouds', colorClass: 'text-slate-500', bgClass: 'bg-slate-800/50' },\n};\n\nexport function getWeatherIcon(condition: WeatherCondition, isNight = false): WeatherIconConfig {\n  const mapped = CONDITION_ICON_MAP[condition.id];\n  if (mapped) {\n    if (isNight && mapped.name === 'clear-day') {\n      return { ...mapped, name: 'clear-night', label: 'Clear Night', colorClass: 'text-indigo-300', bgClass: 'bg-indigo-950/30' };\n    }\n    if (isNight && mapped.name === 'partly-cloudy-day') {\n      return { ...mapped, name: 'partly-cloudy-night', label: 'Partly Cloudy Night' };\n    }\n    return mapped;\n  }\n  // Fallback by main category\n  const main = condition.main.toLowerCase();\n  if (main.includes('thunder')) return CONDITION_ICON_MAP[200];\n  if (main.includes('rain')) return CONDITION_ICON_MAP[501];\n  if (main.includes('snow')) return CONDITION_ICON_MAP[601];\n  if (main.includes('cloud')) return CONDITION_ICON_MAP[803];\n  if (main.includes('clear')) return isNight ? { ...CONDITION_ICON_MAP[800], name: 'clear-night' } : CONDITION_ICON_MAP[800];\n  return { name: 'unknown', label: 'Unknown', colorClass: 'text-slate-400', bgClass: 'bg-slate-800/30' };\n}\n\nconst WIND_DIRECTIONS = [\n  { min: 0, max: 11.25, label: 'N', full: 'North' },\n  { min: 11.25, max: 33.75, label: 'NNE', full: 'North-Northeast' },\n  { min: 33.75, max: 56.25, label: 'NE', full: 'Northeast' },\n  { min: 56.25, max: 78.75, label: 'ENE', full: 'East-Northeast' },\n  { min: 78.75, max: 101.25, label: 'E', full: 'East' },\n  { min: 101.25, max: 123.75, label: 'ESE', full: 'East-Southeast' },\n  { min: 123.75, max: 146.25, label: 'SE', full: 'Southeast' },\n  { min: 146.25, max: 168.75, label: 'SSE', full: 'South-Southeast' },\n  { min: 168.75, max: 191.25, label: 'S', full: 'South' },\n  { min: 191.25, max: 213.75, label: 'SSW', full: 'South-Southwest' },\n  { min: 213.75, max: 236.25, label: 'SW', full: 'Southwest' },\n  { min: 236.25, max: 258.75, label: 'WSW', full: 'West-Southwest' },\n  { min: 258.75, max: 281.25, label: 'W', full: 'West' },\n  { min: 281.25, max: 303.75, label: 'WNW', full: 'West-Northwest' },\n  { min: 303.75, max: 326.25, label: 'NW', full: 'Northwest' },\n  { min: 326.25, max: 348.75, label: 'NNW', full: 'North-Northwest' },\n  { min: 348.75, max: 360, label: 'N', full: 'North' },\n];\n\nexport function getWindDirection(degrees: number): { label: string; full: string; rotation: number } {\n  const normalized = ((degrees % 360) + 360) % 360;\n  const match = WIND_DIRECTIONS.find((d) => normalized >= d.min && normalized < d.max);\n  return {\n    label: match?.label ?? 'N',\n    full: match?.full ?? 'North',\n    rotation: normalized,\n  };\n}\n\nexport function getWindSpeedLabel(speed: number): { label: string; description: string; colorClass: string } {\n  if (speed < 1) return { label: 'Calm', description: 'Calm', colorClass: 'text-slate-400' };\n  if (speed < 6) return { label: 'Light Air', description: 'Smoke drift indicates wind direction', colorClass: 'text-emerald-400' };\n  if (speed < 12) return { label: 'Light Breeze', description: 'Wind felt on face, leaves rustle', colorClass: 'text-emerald-400' };\n  if (speed < 20) return { label: 'Gentle Breeze', description: 'Leaves and small twigs in constant motion', colorClass: 'text-green-400' };\n  if (speed < 29) return { label: 'Moderate Breeze', description: 'Small branches begin to move', colorClass: 'text-yellow-400' };\n  if (speed < 39) return { label: 'Fresh Breeze', description: 'Small trees begin to sway', colorClass: 'text-amber-400' };\n  if (speed < 50) return { label: 'Strong Breeze', description: 'Large branches in motion', colorClass: 'text-orange-400' };\n  if (speed < 62) return { label: 'Near Gale', description: 'Whole trees in motion', colorClass: 'text-orange-500' };\n  if (speed < 75) return { label: 'Gale', description: 'Breaks twigs off trees', colorClass: 'text-red-400' };\n  if (speed < 89) return { label: 'Strong Gale', description: 'Slight structural damage occurs', colorClass: 'text-red-500' };\n  if (speed < 103) return { label: 'Storm', description: 'Trees uprooted; considerable structural damage', colorClass: 'text-red-600' };\n  return { label: 'Hurricane', description: 'Widespread vegetation and structural damage', colorClass: 'text-red-700' };\n}\n\nexport function formatTime(timestamp: number, options?: Intl.DateTimeFormatOptions): string {\n  const date = new Date(timestamp * 1000);\n  return date.toLocaleTimeString('en-US', {\n    hour: 'numeric',\n    minute: '2-digit',\n    hour12: true,\n    ...options,\n  });\n}\n\nexport function formatDate(timestamp: number, options?: Intl.DateTimeFormatOptions): string {\n  const date = new Date(timestamp * 1000);\n  return date.toLocaleDateString('en-US', {\n    weekday: 'long',\n    month: 'long',\n    day: 'numeric',\n    ...options,\n  });\n}\n\nexport function formatShortDate(timestamp: number): string {\n  const date = new Date(timestamp * 1000);\n  const today = new Date();\n  const tomorrow = new Date(today);\n  tomorrow.setDate(today.getDate() + 1);\n  if (date.toDateString() === today.toDateString()) return 'Today';\n  if (date.toDateString() === tomorrow.toDateString()) return 'Tomorrow';\n  return date.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });\n}\n\nexport function formatHour(timestamp: number): string {\n  const date = new Date(timestamp * 1000);\n  const hour = date.getHours();\n  if (hour === 0) return '12 AM';\n  if (hour === 12) return '12 PM';\n  return hour > 12 ? `${hour - 12} PM` : `${hour} AM`;\n}\n\nexport function isNightTime(timestamp: number, sunrise: number, sunset: number): boolean {\n  return timestamp < sunrise || timestamp > sunset;\n}\n\nexport interface AQILabel {\n  label: string;\n  description: string;\n  colorClass: string;\n  bgClass: string;\n  textColorClass: string;\n  range: string;\n}\n\nexport function getAQILabel(category: AirQuality['category']): AQILabel {\n  const map: Record<AirQuality['category'], AQILabel> = {\n    good: {\n      label: 'Good',\n      description: 'Air quality is satisfactory with little or no risk.',\n      colorClass: 'text-emerald-400',\n      bgClass: 'bg-emerald-950/40',\n      textColorClass: 'text-emerald-300',\n      range: '0-50',\n    },\n    moderate: {\n      label: 'Moderate',\n      description: 'Acceptable air quality, but some pollutants may be a concern for sensitive people.',\n      colorClass: 'text-yellow-400',\n      bgClass: 'bg-yellow-950/40',\n      textColorClass: 'text-yellow-300',\n      range: '51-100',\n    },\n    unhealthy_sensitive: {\n      label: 'Unhealthy for Sensitive Groups',\n      description: 'Members of sensitive groups may experience health effects.',\n      colorClass: 'text-orange-400',\n      bgClass: 'bg-orange-950/40',\n      textColorClass: 'text-orange-300',\n      range: '101-150',\n    },\n    unhealthy: {\n      label: 'Unhealthy',\n      description: 'Everyone may begin to experience health effects.',\n      colorClass: 'text-red-400',\n      bgClass: 'bg-red-950/40',\n      textColorClass: 'text-red-300',\n      range: '151-200',\n    },\n    very_unhealthy: {\n      label: 'Very Unhealthy',\n      description: 'Health warnings of emergency conditions for the entire population.',\n      colorClass: 'text-rose-500',\n      bgClass: 'bg-rose-950/50',\n      textColorClass: 'text-rose-300',\n      range: '201-300',\n    },\n    hazardous: {\n      label: 'Hazardous',\n      description: 'Health alert: everyone may experience more serious health effects.',\n      colorClass: 'text-red-700',\n      bgClass: 'bg-red-950/60',\n      textColorClass: 'text-red-200',\n      range: '301+',\n    },\n  };\n  return map[category];\n}\n\nexport interface UVLabel {\n  label: string;\n  description: string;\n  colorClass: string;\n  bgClass: string;\n  recommendation: string;\n  spfAdvice: string;\n}\n\nexport function getUVIndexLabel(category: UVData['category']): UVLabel {\n  const map: Record<UVData['category'], UVLabel> = {\n    low: {\n      label: 'Low',\n      description: 'No protection required. Safe to stay outside.',\n      colorClass: 'text-emerald-400',\n      bgClass: 'bg-emerald-950/40',\n      recommendation: 'Wear sunglasses on bright days.',\n      spfAdvice: 'SPF 15 if you burn easily.',\n    },\n    moderate: {\n      label: 'Moderate',\n      description: 'Some protection needed. Stay in shade near midday.',\n      colorClass: 'text-yellow-400',\n      bgClass: 'bg-yellow-950/40',\n      recommendation: 'Cover up and wear sunscreen.',\n      spfAdvice: 'SPF 30+ recommended.',\n    },\n    high: {\n      label: 'High',\n      description: 'Protection required. Sun damage can occur rapidly.',\n      colorClass: 'text-orange-400',\n      bgClass: 'bg-orange-950/40',\n      recommendation: 'Wear a hat and UV-blocking sunglasses.',\n      spfAdvice: 'SPF 30-50 essential.',\n    },\n    very_high: {\n      label: 'Very High',\n      description: 'Extra protection essential. Avoid sun between 10am-4pm.',\n      colorClass: 'text-red-400',\n      bgClass: 'bg-red-950/40',\n      recommendation: 'Minimize sun exposure during midday hours.',\n      spfAdvice: 'SPF 50+ mandatory. Reapply every 2 hours.',\n    },\n    extreme: {\n      label: 'Extreme',\n      description: 'Maximum protection needed. Unprotected skin burns in minutes.',\n      colorClass: 'text-rose-500',\n      bgClass: 'bg-rose-950/50',\n      recommendation: 'Avoid sun exposure. Stay indoors if possible.',\n      spfAdvice: 'SPF 50+ essential. Protective clothing required.',\n    },\n  };\n  return map[category];\n}\n\nexport function getTemperatureColor(temp: number): string {\n  if (temp <= -10) return 'text-cyan-200';\n  if (temp <= 0) return 'text-cyan-300';\n  if (temp <= 10) return 'text-blue-300';\n  if (temp <= 18) return 'text-sky-300';\n  if (temp <= 24) return 'text-emerald-300';\n  if (temp <= 30) return 'text-amber-300';\n  if (temp <= 36) return 'text-orange-400';\n  return 'text-red-400';\n}\n\nexport function getPressureTrend(pressure: number): { label: string; icon: 'rising' | 'falling' | 'steady'; colorClass: string } {\n  if (pressure > 1023) return { label: 'High Pressure', icon: 'rising', colorClass: 'text-emerald-400' };\n  if (pressure < 1000) return { label: 'Low Pressure', icon: 'falling', colorClass: 'text-red-400' };\n  return { label: 'Normal Pressure', icon: 'steady', colorClass: 'text-slate-400' };\n}\n\nexport function getHumidityLabel(humidity: number): { label: string; description: string; colorClass: string } {\n  if (humidity < 20) return { label: 'Very Dry', description: 'Very low humidity. Stay hydrated.', colorClass: 'text-amber-600' };\n  if (humidity < 40) return { label: 'Dry', description: 'Low humidity. Skin may feel dry.', colorClass: 'text-amber-400' };\n  if (humidity < 60) return { label: 'Comfortable', description: 'Ideal humidity for comfort.', colorClass: 'text-emerald-400' };\n  if (humidity < 80) return { label: 'Humid', description: 'High humidity. May feel sticky.', colorClass: 'text-yellow-400' };\n  return { label: 'Very Humid', description: 'Very high humidity. Heat feels more intense.', colorClass: 'text-orange-400' };\n}\n\nexport function getVisibilityLabel(km: number): { label: string; colorClass: string } {\n  if (km >= 10) return { label: 'Excellent', colorClass: 'text-emerald-400' };\n  if (km >= 5) return { label: 'Good', colorClass: 'text-green-400' };\n  if (km >= 2) return { label: 'Moderate', colorClass: 'text-yellow-400' };\n  if (km >= 1) return { label: 'Poor', colorClass: 'text-orange-400' };\n  return { label: 'Very Poor', colorClass: 'text-red-400' };\n}\n\nexport function getMoonPhaseLabel(phase: number): { label: string; icon: string } {\n  if (phase === 0 || phase === 1) return { label: 'New Moon', icon: 'new-moon' };\n  if (phase < 0.25) return { label: 'Waxing Crescent', icon: 'waxing-crescent' };\n  if (phase === 0.25) return { label: 'First Quarter', icon: 