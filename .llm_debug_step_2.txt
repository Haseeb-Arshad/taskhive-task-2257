```json
{
  "files": [
    {
      "path": "types/weather.ts",
      "content": "export interface Coordinates {\n  lat: number;\n  lon: number;\n}\n\nexport interface CurrentWeather {\n  city: string;\n  country: string;\n  coordinates: Coordinates;\n  temperature: number;\n  feelsLike: number;\n  humidity: number;\n  pressure: number;\n  visibility: number;\n  windSpeed: number;\n  windDegree: number;\n  windGust: number;\n  uvIndex: number;\n  cloudCover: number;\n  dewPoint: number;\n  condition: WeatherCondition;\n  sunrise: number;\n  sunset: number;\n  timestamp: number;\n  timezone: string;\n  high: number;\n  low: number;\n}\n\nexport type WeatherConditionCode =\n  | 'clear'\n  | 'partly-cloudy'\n  | 'cloudy'\n  | 'overcast'\n  | 'fog'\n  | 'drizzle'\n  | 'rain'\n  | 'heavy-rain'\n  | 'thunderstorm'\n  | 'snow'\n  | 'sleet'\n  | 'hail'\n  | 'windy'\n  | 'tornado'\n  | 'blizzard';\n\nexport interface WeatherCondition {\n  code: WeatherConditionCode;\n  label: string;\n  description: string;\n}\n\nexport interface HourlyForecast {\n  timestamp: number;\n  temperature: number;\n  feelsLike: number;\n  humidity: number;\n  windSpeed: number;\n  windDegree: number;\n  precipitationProbability: number;\n  precipitationAmount: number;\n  uvIndex: number;\n  cloudCover: number;\n  condition: WeatherCondition;\n}\n\nexport interface DailyForecast {\n  timestamp: number;\n  high: number;\n  low: number;\n  avgHumidity: number;\n  avgWindSpeed: number;\n  maxWindGust: number;\n  precipitationProbability: number;\n  precipitationAmount: number;\n  uvIndex: number;\n  sunrise: number;\n  sunset: number;\n  moonPhase: number;\n  condition: WeatherCondition;\n  description: string;\n}\n\nexport interface WeatherAlert {\n  id: string;\n  event: string;\n  severity: 'minor' | 'moderate' | 'severe' | 'extreme';\n  headline: string;\n  description: string;\n  instruction: string;\n  start: number;\n  end: number;\n  sender: string;\n  areas: string[];\n}\n\nexport interface AirQuality {\n  aqi: number;\n  category: AQICategory;\n  dominantPollutant: string;\n  components: {\n    co: number;\n    no: number;\n    no2: number;\n    o3: number;\n    so2: number;\n    pm2_5: number;\n    pm10: number;\n    nh3: number;\n  };\n  healthImplication: string;\n  cautionaryStatement: string;\n}\n\nexport type AQICategory =\n  | 'Good'\n  | 'Fair'\n  | 'Moderate'\n  | 'Poor'\n  | 'Very Poor'\n  | 'Hazardous';\n\nexport interface SunMoonData {\n  sunrise: number;\n  sunset: number;\n  solarNoon: number;\n  dayLength: number;\n  civilTwilightBegin: number;\n  civilTwilightEnd: number;\n  moonPhase: number;\n  moonPhaseName: MoonPhaseName;\n  moonrise: number;\n  moonset: number;\n  goldenHourMorning: number;\n  goldenHourEvening: number;\n}\n\nexport type MoonPhaseName =\n  | 'New Moon'\n  | 'Waxing Crescent'\n  | 'First Quarter'\n  | 'Waxing Gibbous'\n  | 'Full Moon'\n  | 'Waning Gibbous'\n  | 'Last Quarter'\n  | 'Waning Crescent';\n\nexport interface PollenData {\n  overall: number;\n  tree: number;\n  grass: number;\n  weed: number;\n  mold: number;\n  label: 'Low' | 'Moderate' | 'High' | 'Very High';\n}\n\nexport interface WeatherData {\n  current: CurrentWeather;\n  hourly: HourlyForecast[];\n  daily: DailyForecast[];\n  alerts: WeatherAlert[];\n  airQuality: AirQuality;\n  sunMoon: SunMoonData;\n  pollen: PollenData;\n  lastUpdated: number;\n}\n\nexport interface SearchResult {\n  city: string;\n  country: string;\n  state?: string;\n  coordinates: Coordinates;\n  timezone: string;\n}\n\nexport type TemperatureUnit = 'celsius' | 'fahrenheit';\nexport type WindSpeedUnit = 'mph' | 'kph' | 'ms' | 'knots';\nexport type PressureUnit = 'hPa' | 'inHg' | 'mb';\n\nexport interface UserPreferences {\n  temperatureUnit: TemperatureUnit;\n  windSpeedUnit: WindSpeedUnit;\n  pressureUnit: PressureUnit;\n  use24HourTime: boolean;\n}\n"
    },
    {
      "path": "lib/weatherService.ts",
      "content": "import type {\n  WeatherData,\n  CurrentWeather,\n  HourlyForecast,\n  DailyForecast,\n  WeatherAlert,\n  AirQuality,\n  SunMoonData,\n  PollenData,\n  WeatherCondition,\n  WeatherConditionCode,\n  SearchResult,\n} from '@/types/weather';\n\n// ---------------------------------------------------------------------------\n// Deterministic seeded pseudo-random helpers\n// ---------------------------------------------------------------------------\nfunction hashString(str: string): number {\n  let hash = 5381;\n  for (let i = 0; i < str.length; i++) {\n    hash = (hash << 5) + hash + str.charCodeAt(i);\n    hash = hash & hash; // Convert to 32bit integer\n  }\n  return Math.abs(hash);\n}\n\nfunction seededRandom(seed: number, index: number = 0): number {\n  const x = Math.sin(seed * 9301 + index * 49297 + 233) * 43758.5453;\n  return x - Math.floor(x);\n}\n\nfunction randomBetween(seed: number, index: number, min: number, max: number): number {\n  return min + seededRandom(seed, index) * (max - min);\n}\n\nfunction randomInt(seed: number, index: number, min: number, max: number): number {\n  return Math.round(randomBetween(seed, index, min, max));\n}\n\n// ---------------------------------------------------------------------------\n// City database - realistic baseline data\n// ---------------------------------------------------------------------------\ninterface CityProfile {\n  city: string;\n  country: string;\n  state?: string;\n  timezone: string;\n  coordinates: { lat: number; lon: number };\n  climate: {\n    avgTempC: number;\n    tempRange: number;\n    humidity: number;\n    rainChance: number;\n  };\n}\n\nconst CITY_DATABASE: Record<string, CityProfile> = {\n  'new york': {\n    city: 'New York',\n    country: 'US',\n    state: 'New York',\n    timezone: 'America/New_York',\n    coordinates: { lat: 40.7128, lon: -74.006 },\n    climate: { avgTempC: 14, tempRange: 10, humidity: 65, rainChance: 35 },\n  },\n  london: {\n    city: 'London',\n    country: 'GB',\n    timezone: 'Europe/London',\n    coordinates: { lat: 51.5074, lon: -0.1278 },\n    climate: { avgTempC: 11, tempRange: 7, humidity: 78, rainChance: 45 },\n  },\n  tokyo: {\n    city: 'Tokyo',\n    country: 'JP',\n    timezone: 'Asia/Tokyo',\n    coordinates: { lat: 35.6762, lon: 139.6503 },\n    climate: { avgTempC: 16, tempRange: 8, humidity: 72, rainChance: 40 },\n  },\n  paris: {\n    city: 'Paris',\n    country: 'FR',\n    timezone: 'Europe/Paris',\n    coordinates: { lat: 48.8566, lon: 2.3522 },\n    climate: { avgTempC: 12, tempRange: 9, humidity: 70, rainChance: 38 },\n  },\n  sydney: {\n    city: 'Sydney',\n    country: 'AU',\n    state: 'New South Wales',\n    timezone: 'Australia/Sydney',\n    coordinates: { lat: -33.8688, lon: 151.2093 },\n    climate: { avgTempC: 18, tempRange: 8, humidity: 68, rainChance: 30 },\n  },\n  dubai: {\n    city: 'Dubai',\n    country: 'AE',\n    timezone: 'Asia/Dubai',\n    coordinates: { lat: 25.2048, lon: 55.2708 },\n    climate: { avgTempC: 30, tempRange: 5, humidity: 55, rainChance: 5 },\n  },\n  moscow: {\n    city: 'Moscow',\n    country: 'RU',\n    timezone: 'Europe/Moscow',\n    coordinates: { lat: 55.7558, lon: 37.6173 },\n    climate: { avgTempC: 5, tempRange: 15, humidity: 75, rainChance: 42 },\n  },\n  'los angeles': {\n    city: 'Los Angeles',\n    country: 'US',\n    state: 'California',\n    timezone: 'America/Los_Angeles',\n    coordinates: { lat: 34.0522, lon: -118.2437 },\n    climate: { avgTempC: 19, tempRange: 6, humidity: 68, rainChance: 15 },\n  },\n  chicago: {\n    city: 'Chicago',\n    country: 'US',\n    state: 'Illinois',\n    timezone: 'America/Chicago',\n    coordinates: { lat: 41.8781, lon: -87.6298 },\n    climate: { avgTempC: 10, tempRange: 14, humidity: 71, rainChance: 38 },\n  },\n  mumbai: {\n    city: 'Mumbai',\n    country: 'IN',\n    timezone: 'Asia/Kolkata',\n    coordinates: { lat: 19.076, lon: 72.8777 },\n    climate: { avgTempC: 27, tempRange: 5, humidity: 80, rainChance: 50 },\n  },\n  berlin: {\n    city: 'Berlin',\n    country: 'DE',\n    timezone: 'Europe/Berlin',\n    coordinates: { lat: 52.52, lon: 13.405 },\n    climate: { avgTempC: 10, tempRange: 10, humidity: 74, rainChance: 40 },\n  },\n  toronto: {\n    city: 'Toronto',\n    country: 'CA',\n    state: 'Ontario',\n    timezone: 'America/Toronto',\n    coordinates: { lat: 43.6532, lon: -79.3832 },\n    climate: { avgTempC: 9, tempRange: 13, humidity: 70, rainChance: 36 },\n  },\n  singapore: {\n    city: 'Singapore',\n    country: 'SG',\n    timezone: 'Asia/Singapore',\n    coordinates: { lat: 1.3521, lon: 103.8198 },\n    climate: { avgTempC: 28, tempRange: 3, humidity: 84, rainChance: 55 },\n  },\n  cairo: {\n    city: 'Cairo',\n    country: 'EG',\n    timezone: 'Africa/Cairo',\n    coordinates: { lat: 30.0444, lon: 31.2357 },\n    climate: { avgTempC: 22, tempRange: 8, humidity: 45, rainChance: 8 },\n  },\n  'sao paulo': {\n    city: 'Sao Paulo',\n    country: 'BR',\n    timezone: 'America/Sao_Paulo',\n    coordinates: { lat: -23.5505, lon: -46.6333 },\n    climate: { avgTempC: 20, tempRange: 6, humidity: 78, rainChance: 48 },\n  },\n};\n\nconst DEFAULT_CITY: CityProfile = {\n  city: 'New York',\n  country: 'US',\n  state: 'New York',\n  timezone: 'America/New_York',\n  coordinates: { lat: 40.7128, lon: -74.006 },\n  climate: { avgTempC: 14, tempRange: 10, humidity: 65, rainChance: 35 },\n};\n\n// ---------------------------------------------------------------------------\n// Weather condition logic\n// ---------------------------------------------------------------------------\nconst CONDITIONS: Record<WeatherConditionCode, Omit<WeatherCondition, 'code'>> = {\n  clear: { label: 'Clear Sky', description: 'Clear and sunny conditions' },\n  'partly-cloudy': { label: 'Partly Cloudy', description: 'Mix of sun and clouds' },\n  cloudy: { label: 'Cloudy', description: 'Mostly cloudy skies' },\n  overcast: { label: 'Overcast', description: 'Completely overcast skies' },\n  fog: { label: 'Foggy', description: 'Dense fog reducing visibility' },\n  drizzle: { label: 'Drizzle', description: 'Light drizzle' },\n  rain: { label: 'Rain', description: 'Moderate rainfall expected' },\n  'heavy-rain': { label: 'Heavy Rain', description: 'Heavy rainfall, flooding possible' },\n  thunderstorm: { label: 'Thunderstorm', description: 'Thunder and lightning activity' },\n  snow: { label: 'Snow', description: 'Snowfall expected' },\n  sleet: { label: 'Sleet', description: 'Mixed rain and snow' },\n  hail: { label: 'Hail', description: 'Hailstones possible' },\n  windy: { label: 'Windy', description: 'Strong winds, gusts possible' },\n  tornado: { label: 'Tornado Warning', description: 'Tornado conditions present' },\n  blizzard: { label: 'Blizzard', description: 'Severe blizzard conditions' },\n};\n\nfunction getConditionFromMetrics(\n  rainChance: number,\n  cloudCover: number,\n  temp: number,\n  windSpeed: number,\n  seed: number,\n  index: number\n): WeatherCondition {\n  const roll = seededRandom(seed, index + 500);\n\n  let code: WeatherConditionCode;\n  \n  if (windSpeed > 60 && roll < 0.3) {\n    code = 'tornado';\n  } else if (temp < -5 && rainChance > 60) {\n    code = 'blizzard';\n  } else if (rainChance > 80) {\n    code = roll < 0.3 ? 'thunderstorm' : 'heavy-rain';\n  } else if (rainChance > 55) {\n    code = roll < 0.4 ? 'rain' : temp < 2 ? 'sleet' : 'rain';\n  } else if (rainChance > 30) {\n    code = roll < 0.5 ? 'drizzle' : 'rain';\n  } else if (temp < 0 && rainChance > 20) {\n    code = 'snow';\n  } else if (windSpeed > 40) {\n    code = 'windy';\n  } else if (cloudCover > 85) {\n    code = roll < 0.4 ? 'overcast' : 'fog';\n  } else if (cloudCover > 55) {\n    code = 'cloudy';\n  } else if (cloudCover > 25) {\n    code = 'partly-cloudy';\n  } else {\n    code = 'clear';\n  }\n\n  return { code, ...CONDITIONS[code] };\n}\n\n// ---------------------------------------------------------------------------\n// Individual data generators\n// ---------------------------------------------------------------------------\nfunction generateCurrentWeather(profile: CityProfile, seed: number): CurrentWeather {\n  const now = Math.floor(Date.now() / 1000);\n  const { climate } = profile;\n\n  const baseTemp = climate.avgTempC + randomBetween(seed, 1, -climate.tempRange, climate.tempRange);\n  const tempCelsius = parseFloat(baseTemp.toFixed(1));\n  const humidity = Math.round(climate.humidity + randomBetween(seed, 2, -10, 10));\n  const rainChance = Math.min(100, Math.round(climate.rainChance + randomBetween(seed, 3, -15, 25)));\n  const cloudCover = Math.round(rainChance * 0.8 + seededRandom(seed, 4) * 30);\n  const windSpeed = parseFloat(randomBetween(seed, 5, 5, 35).toFixed(1));\n  const windDegree = randomInt(seed, 6, 0, 359);\n  const windGust = parseFloat((windSpeed + randomBetween(seed, 7, 0, 15)).toFixed(1));\n  const pressure = parseFloat((1013 + randomBetween(seed, 8, -20, 20)).toFixed(1));\n  const uvIndex = parseFloat(randomBetween(seed, 9, 0, 11).toFixed(1));\n  const visibility = parseFloat(randomBetween(seed, 10, 5, 25).toFixed(1));\n  const dewPoint = parseFloat((tempCelsius - (100 - humidity) / 5).toFixed(1));\n  const feelsLike = parseFloat(\n    (tempCelsius - windSpeed * 0.05 + (humidity - 60) * 0.05).toFixed(1)\n  );\n\n  // Sunrise/sunset: rough calculation\n  const todayMidnight = Math.floor(now / 86400) * 86400;\n  const sunrise = todayMidnight + 6 * 3600 + randomInt(seed, 11, -1800, 1800);\n  const sunset = todayMidnight + 18 * 3600 + randomInt(seed, 12, -1800, 1800);\n\n  const high = parseFloat((tempCelsius + randomBetween(seed, 13, 3, 8)).toFixed(1));\n  const low = parseFloat((tempCelsius - randomBetween(seed, 14, 3, 8)).toFixed(1));\n\n  const condition = getConditionFromMetrics(rainChance, cloudCover, tempCelsius, windSpeed, seed, 15);\n\n  return {\n    city: profile.city,\n    country: profile.country,\n    coordinates: profile.coordinates,\n    temperature: tempCelsius,\n    feelsLike,\n    humidity,\n    pressure,\n    visibility,\n    windSpeed,\n    windDegree,\n    windGust,\n    uvIndex,\n    cloudCover: Math.min(100, cloudCover),\n    dewPoint,\n    condition,\n    sunrise,\n    sunset,\n    timestamp: now,\n    timezone: profile.timezone,\n    high,\n    low,\n  };\n}\n\nfunction generateHourlyForecast(profile: CityProfile, seed: number): HourlyForecast[] {\n  const now = Math.floor(Date.now() / 1000);\n  const currentHour = Math.floor(now / 3600) * 3600;\n  const { climate } = profile;\n\n  return Array.from({ length: 48 }, (_, i) => {\n    const hourSeed = seed + i * 137;\n    const hourOfDay = ((currentHour / 3600) + i) % 24;\n\n    // Temperature follows a daily curve: cooler at night, warmer midday\n    const dailyCurve = Math.sin(((hourOfDay - 6) / 24) * 2 * Math.PI) * climate.tempRange * 0.4;\n    const baseTemp = climate.avgTempC + dailyCurve + randomBetween(hourSeed, 1, -2, 2);\n    const temperature = parseFloat(baseTemp.toFixed(1));\n    const humidity = Math.min(100, Math.round(climate.humidity + randomBetween(hourSeed, 2, -8, 8)));\n    const rainChance = Math.min(100, Math.round(climate.rainChance + randomBetween(hourSeed, 3, -10, 20)));\n    const cloudCover = Math.min(100, Math.round(rainChance * 0.7 + seededRandom(hourSeed, 4) * 30));\n    const windSpeed = parseFloat(randomBetween(hourSeed, 5, 5, 30).toFixed(1));\n    const windDegree = randomInt(hourSeed, 6, 0, 359);\n    const precipAmount = rainChance > 30 ? parseFloat(randomBetween(hourSeed, 7, 0, 5).toFixed(2)) : 0;\n\n    // UV only during day hours\n    const uvIndex = hourOfDay >= 7 && hourOfDay <= 19\n      ? parseFloat(randomBetween(hourSeed, 8, 0, 10).toFixed(1))\n      : 0;\n\n    const feelsLike = parseFloat((temperature - windSpeed * 0.05).toFixed(1));\n    const condition = getConditionFromMetrics(rainChance, cloudCover, temperature, windSpeed, hourSeed, 9);\n\n    return {\n      timestamp: currentHour + i * 3600,\n      temperature,\n      feelsLike,\n      humidity,\n      windSpeed,\n      windDegree,\n      precipitationProbability: rainChance,\n      precipitationAmount: precipAmount,\n      uvIndex,\n      cloudCover,\n      condition,\n    };\n  });\n}\n\nfunction generateDailyForecast(profile: CityProfile, seed: number): DailyForecast[] {\n  const now = Math.floor(Date.now() / 1000);\n  const todayMidnight = Math.floor(now / 86400) * 86400;\n  const { climate } = profile;\n\n  const dayDescriptions = [\n    'Conditions remain stable throughout the day.',\n    'Gradual clearing expected by afternoon.',\n    'Morning showers giving way to partial sun.',\n    'Warm and pleasant, ideal for outdoor activities.',\n    'Unsettled pattern continues with periodic showers.',\n    'High pressure builds, bringing drier conditions.',\n    'Humid and muggy with afternoon convection possible.',\n    'Cooler than average with gusty northerly winds.',\n  ];\n\n  return Array.from({ length: 10 }, (_, i) => {\n    const daySeed = seed + i * 317;\n    const baseTemp = climate.avgTempC + randomBetween(daySeed, 1, -climate.tempRange, climate.tempRange);\n    const high = parseFloat((baseTemp + randomBetween(daySeed, 2, 3, 9)).toFixed(1));\n    const low = parseFloat((baseTemp - randomBetween(daySeed, 3, 3, 9)).toFixed(1));\n    const avgHumidity = Math.min(100, Math.round(climate.humidity + randomBetween(daySeed, 4, -10, 10)));\n    const rainChance = Math.min(100, Math.round(climate.rainChance + randomBetween(daySeed, 5, -15, 30)));\n    const cloudCover = Math.min(100, Math.round(rainChance * 0.8 + seededRandom(daySeed, 6) * 20));\n    const avgWindSpeed = parseFloat(randomBetween(daySeed, 7, 8, 30).toFixed(1));\n    const maxWindGust = parseFloat((avgWindSpeed + randomBetween(daySeed, 8, 5, 20)).toFixed(1));\n    const precipAmount = rainChance > 30 ? parseFloat(randomBetween(daySeed, 9, 0, 20).toFixed(1)) : 0;\n    const uvIndex = parseFloat(randomBetween(daySeed, 10, 1, 10).toFixed(1));\n    const moonPhase = parseFloat(((i * 0.033) % 1).toFixed(2));\n\n    const todayMidnightLocal = todayMidnight + i * 86400;\n    const sunrise = todayMidnightLocal + 6 * 3600 + randomInt(daySeed, 11, -1800, 1800);\n    const sunset = todayMidnightLocal + 18 * 3600 + randomInt(daySeed, 12, -1800, 1800);\n\n    const condition = getConditionFromMetrics(rainChance, cloudCover, high, avgWindSpeed, daySeed, 13);\n    const description = dayDescriptions[randomInt(daySeed, 14, 0, dayDescriptions.length - 1)];\n\n    return {\n      timestamp: todayMidnightLocal,\n      high,\n      low,\n      avgHumidity,\n      avgWindSpeed,\n      maxWindGust,\n      precipitationProbability: rainChance,\n      precipitationAmount: precipAmount,\n      uvIndex,\n      sunrise,\n      sunset,\n      moonPhase,\n      condition,\n      description,\n    };\n  });\n}\n\nfunction generateAlerts(profile: CityProfile, seed: number): WeatherAlert[] {\n  const now = Math.floor(Date.now() / 1000);\n  const roll = seededRandom(seed, 99);\n\n  // ~20% chance of having an alert for any city\n  if (roll > 0.2) return [];\n\n  const alertTypes = [\n    {\n      event: 'Wind Advisory',\n      severity: 'minor' as const,\n      headline: 'Wind Advisory in effect until this evening',\n      description: 'Sustained winds of 25 to 35 mph with gusts up to 50 mph expected. Gusty winds could blow around unsecured objects.',\n      instruction: 'Secure outdoor furniture and objects. Be cautious driving high-profile vehicles.',\n      sender: 'NWS Weather Forecast Office',\n    },\n    {\n      event: 'Flash Flood Watch',\n      severity: 'moderate' as const,\n      headline: 'Flash Flood Watch through tomorrow morning',\n      description: 'Heavy rainfall of 2 to 4 inches possible over the next 12 hours. Low-lying areas and poor drainage zones may experience flooding.',\n      instruction: 'Monitor local forecasts. Avoid driving through flooded roadways.',\n      sender: 'NWS River Forecast Center',\n    },\n    {\n      event: 'Heat Advisory',\n      severity: 'moderate' as const,\n      headline: 'Heat Advisory in effect from noon until 8 PM local time',\n      description: 'Heat index values up to 105°F expected. Hot conditions may cause heat illness.',\n      instruction: 'Drink plenty of fluids. Stay in air-conditioned rooms. Avoid outdoor activities during peak heat.',\n      sender: 'NWS Weather Forecast Office',\n    },\n    {\n      event: 'Severe Thunderstorm Warning',\n      severity: 'severe' as const,\n      headline: 'Severe Thunderstorm Warning until 4:30 PM',\n      description: 'A severe thunderstorm capable of producing golf ball size hail and damaging winds in excess of 60 mph.',\n      instruction: 'Move to an interior room on the lowest floor of a building. Stay away from windows.',\n      sender: 'NWS Storm Prediction Center',\n    },\n  ];\n\n  const alertIndex = randomInt(seed, 100, 0, alertTypes.length - 1);\n  const alertData = alertTypes[alertIndex];\n\n  return [\n    {\n      id: `alert-${seed}-${alertIndex}`,\n      ...alertData,\n      start: now - 3600,\n      end: now + 18 * 3600,\n      areas: [profile.city, `${profile.city} Metro Area`],\n    },\n  ];\n}\n\nfunction generateAirQuality(seed: number): AirQuality {\n  const aqi = randomInt(seed, 200, 1, 200);\n\n  type AQICategoryDef = {\n    category: AirQuality['category'];\n    healthImplication: string;\n    cautionaryStatement: string;\n  };\n\n  const getCategory = (aqi: number): AQICategoryDef => {\n    if (aqi <= 50)\n      return {\n        category: 'Good',\n        healthImplication: 'Air quality is satisfactory, and air pollution poses little or no risk.',\n        cautionaryStatement: 'None.',\n      };\n    if (aqi <= 100)\n      return {\n        category: 'Fair',\n        healthImplication: 'Air quality is acceptable. However, there may be some concern for a small number of sensitive individuals.',\n        cautionaryStatement: 'Active children and adults, and people with respiratory disease, should limit prolonged outdoor exertion.',\n      };\n    if (aqi <= 150)\n      return {\n        category: 'Moderate',\n        healthImplication: 'Members of sensitive groups may experience health effects. The general public is not likely to be affected.',\n        cautionaryStatement: 'Active children and adults, and people with respiratory disease, should avoid prolonged outdoor exertion.',\n      };\n    if (aqi <= 200)\n      return {\n        category: 'Poor',\n        healthImplication: 'Some members of the general public may experience health effects; sensitive groups may experience more serious effects.',\n        cautionaryStatement: 'Active children and adults, and people with respiratory disease, should avoid prolonged outdoor exertion; everyone else should reduce prolonged outdoor exertion.',\n      };\n    if (aqi <= 300)\n      return {\n        category: 'Very Poor',\n        healthImplication: 'Health alert: everyone may experience more serious health effects.',\n        cautionaryStatement: 'Everyone should avoid all outdoor exertion.',\n      };\n    return {\n      category: 'Hazardous',\n      healthImplication: 'Health warnings of emergency conditions. The entire population is more likely to be affected.',\n      cautionaryStatement: 'Everyone should avoid all outdoor exertion.',\n    };\n  };\n\n  const categoryData = getCategory(aqi);\n  const pollutants = ['PM2.5', 'PM10', 'O3', 'NO2', 'SO2', 'CO'];\n  const dominantPollutant = pollutants[randomInt(seed, 201, 0, pollutants.length - 1)];\n\n  return {\n    aqi,\n    category: categoryData.category,\n    dominantPollutant,\n    components: {\n      co: parseFloat(randomBetween(seed, 202, 200, 1000).toFixed(2)),\n      no: parseFloat(randomBetween(seed, 203, 0, 30).toFixed(2)),\n      no2: parseFloat(randomBetween(seed, 204, 5, 80).toFixed(2)),\n      o3: parseFloat(randomBetween(seed, 205, 20, 120).toFixed(2)),\n      so2: parseFloat(randomBetween(seed, 206, 1, 40).toFixed(2)),\n      pm2_5: parseFloat(randomBetween(seed, 207, 2, 80).toFixed(2)),\n      pm10: parseFloat(randomBetween(seed, 208, 5, 150).toFixed(2)),\n      nh3: parseFloat(randomBetween(seed, 209, 0.5, 25).toFixed(2)),\n    },\n    healthImplication: categoryData.healthImplication,\n    cautionaryStatement: categoryData.cautionaryStatement,\n  };\n}\n\nfunction generateSunMoonData(seed: number, timezone: string): SunMoonData {\n  const now = Math.floor(Date.now() / 1000);\n  const todayMidnight = Math.floor(now / 86400) * 86400;\n\n  const sunrise = todayMidnight + 6 * 3600 + randomInt(seed, 300, -2400, 2400);\n  const sunset = todayMidnight + 18 * 3600 + randomInt(seed, 301, -2400, 2400);\n  const solarNoon = Math.round((sunrise + sunset) / 2);\n  const dayLength = sunset - sunrise;\n  const civilTwilightBegin = sunrise - randomInt(seed, 302, 1200, 2400);\n  const civilTwilightEnd = sunset + randomInt(seed, 303, 1200, 2400);\n  const goldenHourMorning = sunrise + randomInt(seed, 304, 1800, 3600);\n  const goldenHourEvening = sunset - randomInt(seed, 305, 1800, 3600);\n\n  const moonPhase = parseFloat(seededRandom(seed, 306).toFixed(2));\n  const moonPhaseNames: SunMoonData['moonPhaseName'][] = [\n    'New Moon',\n    'Waxing Crescent',\n    'First Quarter',\n    'Waxing Gibbous',\n    'Full Moon',\n    'Waning Gibbous',\n    'Last Quarter',\n    'Waning Crescent',\n  ];\n  const moonPhaseIndex = Math.floor(moonPhase * 8) % 8;\n  const moonPhaseName = moonPhaseNames[moonPhaseIndex];\n\n  const moonrise = todayMidnight + randomInt(seed, 307, 14400, 72000);\n  const moonset = moonrise + randomInt(seed, 308, 32400, 50400);\n\n  return {\n    sunrise,\n    sunset,\n    solarNoon,\n    dayLength,\n    civilTwilightBegin,\n    civilTwilightEnd,\n    moonPhase,\n    moonPhaseName,\n    moonrise,\n    moonset,\n    goldenHourMorning,\n    goldenHourEvening,\n  };\n}\n\nfunction generatePollenData(seed: number): PollenData {\n  const tree = randomInt(seed, 400, 0, 10);\n  const grass = randomInt(seed, 401, 0, 10);\n  const weed = randomInt(seed, 402, 0, 10);\n  const mold = randomInt(seed, 403, 0, 10);\n  const overall = Math.round((tree + grass + weed + mold) / 4);\n\n  const label: PollenData['label'] =\n    overall <= 2 ? 'Low' : overall <= 5 ? 'Moderate' : overall <= 8 ? 'High' : 'Very High';\n\n  return { overall, tree, grass, weed, mold, label };\n}\n\n// ---------------------------------------------------------------------------\n// Public API\n// ---------------------------------------------------------------------------\nexport function getWeatherData(cityQuery: string): WeatherData {\n  const normalizedQuery = cityQuery.toLowerCase().trim();\n  const profile = CITY_DATABASE[normalizedQuery] ?? DEFAULT_CITY;\n\n  // Generate a stable seed from the city name + today's date so data is\n  // consistent within the same calendar day but changes day-to-day.\n  const today = new Date();\n  const dateStr = `${today.getFullYear()}-${today.getMonth()}-${today.getDate()}`;\n  const seed = hashString(`${profile.city}-${dateStr}`);\n\n  const current = generateCurrentWeather(profile, seed);\n  const hourly = generateHourlyForecast(profile, seed);\n  const daily = generateDailyForecast(profile, seed);\n  const alerts = generateAlerts(profile, seed);\n  const airQuality = generateAirQuality(seed);\n  const sunMoon = generateSunMoonData(seed, profile.timezone);\n  const pollen = generatePollenData(seed);\n\n  return {\n    current,\n    hourly,\n    daily,\n    alerts,\n    airQuality,\n    sunMoon,\n    pollen,\n    lastUpdated: Math.floor(Date.now() / 1000),\n  };\n}\n\nexport function searchCities(query: string): SearchResult[] {\n  if (!query || query.length < 2) return [];\n\n  const normalized = query.toLowerCase().trim();\n\n  return Object.entries(CITY_DATABASE)\n    .filter(([key, profile]) =>\n      key.includes(normalized) ||\n      profile.city.toLowerCase().includes(normalized) ||\n      profile.country.toLowerCase().includes(normalized)\n    )\n    .slice(0, 8)\n    .map(([, profile]) => ({\n      city: profile.city,\n      country: profile.country,\n      state: profile.state,\n      coordinates: profile.coordinates,\n      timezone: profile.timezone,\n    }));\n}\n\nexport function getAllCities(): SearchResult[] {\n  return Object.values(CITY_DATABASE).map((profile) => ({\n    city: profile.city,\n    country: profile.country,\n    state: profile.state,\n    coordinates: profile.coordinates,\n    timezone: profile.timezone,\n  }));\n}\n"
    },
    {
      "path": "lib/weatherUtils.ts",
      "content": "import type {\n  WeatherConditionCode,\n  AQICategory,\n  TemperatureUnit,\n  WindSpeedUnit,\n  PressureUnit,\n  MoonPhaseName,\n} from '@/types/weather';\n\n// ---------------------------------------------------------------------------\n// Weather Icon Mapping\n// ---------------------------------------------------------------------------\nexport type WeatherIconName =\n  | 'sun'\n  | 'cloud-sun'\n  | 'cloud'\n  | 'clouds'\n  | 'fog'\n  | 'cloud-drizzle'\n  | 'cloud-rain'\n  | 'cloud-rain-heavy'\n  | 'cloud-lightning'\n  | 'cloud-snow'\n  | 'cloud-sleet'\n  | 'cloud-hail'\n  | 'wind'\n  | 'tornado'\n  | 'snowflake';\n\nexport interface WeatherIconMeta {\n  icon: WeatherIconName;\n  /** Phosphor icon weight suited for this condition */\n  weight: 'thin' | 'light' | 'regular' | 'bold' | 'fill' | 'duotone';\n  /** Tailwind color class for the icon */\n  colorClass: string;\n  /** Accessible label for screen-readers */\n  label: string;\n}\n\nexport function getWeatherIcon(code: WeatherConditionCode, isDaytime = true): WeatherIconMeta {\n  const iconMap: Record<WeatherConditionCode, WeatherIconMeta> = {\n    clear: {\n      icon: 'sun',\n      weight: 'fill',\n      colorClass: isDaytime ? 'text-amber-400' : 'text-indigo-300',\n      label: isDaytime ? 'Clear sky' : 'Clear night',\n    },\n    'partly-cloudy': {\n      icon: 'cloud-sun',\n      weight: 'fill',\n      colorClass: 'text-amber-300',\n      label: 'Partly cloudy',\n    },\n    cloudy: {\n      icon: 'cloud',\n      weight: 'fill',\n      colorClass: 'text-slate-400',\n      label: 'Cloudy',\n    },\n    overcast: {\n      icon: 'clouds',\n      weight: 'fill',\n      colorClass: 'text-slate-500',\n      label: 'Overcast',\n    },\n    fog: {\n      icon: 'fog',\n      weight: 'regular',\n      colorClass: 'text-slate-400',\n      label: 'Foggy',\n    },\n    drizzle: {\n      icon: 'cloud-drizzle',\n      weight: 'fill',\n      colorClass: 'text-sky-400',\n      label: 'Drizzle',\n    },\n    rain: {\n      icon: 'cloud-rain',\n      weight: 'fill',\n      colorClass: 'text-sky-500',\n      label: 'Rain',\n    },\n    'heavy-rain': {\n      icon: 'cloud-rain-heavy',\n      weight: 'fill',\n      colorClass: 'text-blue-600',\n      label: 'Heavy rain',\n    },\n    thunderstorm: {\n      icon: 'cloud-lightning',\n      weight: 'fill',\n      colorClass: 'text-violet-500',\n      label: 'Thunderstorm',\n    },\n    snow: {\n      icon: 'cloud-snow',\n      weight: 'fill',\n      colorClass: 'text-sky-200',\n      label: 'Snow',\n    },\n    sleet: {\n      icon: 'cloud-sleet',\n      weight: 'fill',\n      colorClass: 'text-sky-300',\n      label: 'Sleet',\n    },\n    hail: {\n      icon: 'cloud-hail',\n      weight: 'fill',\n      colorClass: 'text-cyan-400',\n      label: 'Hail',\n    },\n    windy: {\n      icon: 'wind',\n      weight: 'regular',\n      colorClass: 'text-teal-400',\n      label: 'Windy',\n    },\n    tornado: {\n      icon: 'tornado',\n      weight: 'fill',\n      colorClass: 'text-rose-600',\n      label: 'Tornado warning',\n    },\n    blizzard: {\n      icon: 'snowflake',\n      weight: 'fill',\n      colorClass: 'text-blue-200',\n      label: 'Blizzard',\n    },\n  };\n\n  return iconMap[code];\n}\n\n// ---------------------------------------------------------------------------\n// Wind Direction\n// ---------------------------------------------------------------------------\nexport interface WindDirectionMeta {\n  cardinal: string;\n  shorthand: string;\n  degrees: number;\n  description: string;\n}\n\nexport function getWindDirection(degrees: number): WindDirectionMeta {\n  // Normalize to 0-359\n  const normalized = ((degrees % 360) + 360) % 360;\n  const index = Math.round(normalized / 22.5) % 16;\n\n  const directions: Array<Omit<WindDirectionMeta, 'degrees'>> = [\n    { cardinal: 'North', shorthand: 'N', description: 'Northerly wind' },\n    { cardinal: 'North-Northeast', shorthand: 'NNE', description: 'North-northeasterly wind' },\n    { cardinal: 'Northeast', shorthand: 'NE', description: 'Northeasterly wind' },\n    { cardinal: 'East-Northeast', shorthand: 'ENE', description: 'East-northeasterly wind' },\n    { cardinal: 'East', shorthand: 'E', description: 'Easterly wind' },\n    { cardinal: 'East-Southeast', shorthand: 'ESE', description: 'East-southeasterly wind' },\n    { cardinal: 'Southeast', shorthand: 'SE', description: 'Southeasterly wind' },\n    { cardinal: 'South-Southeast', shorthand: 'SSE', description: 'South-southeasterly wind' },\n    { cardinal: 'South', shorthand: 'S', description: 'Southerly wind' },\n    { cardinal: 'South-Southwest', shorthand: 'SSW', description: 'South-southwesterly wind' },\n    { cardinal: 'Southwest', shorthand: 'SW', description: 'Southwesterly wind' },\n    { cardinal: 'West-Southwest', shorthand: 'WSW', description: 'West-southwesterly wind' },\n    { cardinal: 'West', shorthand: 'W', description: 'Westerly wind' },\n    { cardinal: 'West-Northwest', shorthand: 'WNW', description: 'West-northwesterly wind' },\n    { cardinal: 'Northwest', shorthand: 'NW', description: 'Northwesterly wind' },\n    { cardinal: 'North-Northwest', shorthand: 'NNW', description: 'North-northwesterly wind' },\n  ];\n\n  return { ...directions[index], degrees: normalized };\n}\n\n// ---------------------------------------------------------------------------\n// Time formatting\n// ---------------------------------------------------------------------------\nexport interface FormatTimeOptions {\n  use24Hour?: boolean;\n  showSeconds?: boolean;\n  timezone?: string;\n}\n\nexport function formatTime(unixTimestamp: number, options: FormatTimeOptions = {}): string {\n  const { use24Hour = false, showSeconds = false, timezone } = options;\n  const date = new Date(unixTimestamp * 1000);\n\n  try {\n    return date.toLocaleTimeString('en-US', {\n      hour: '2-digit',\n      minute: '2-digit',\n      second: showSeconds ? '2-digit' : undefined,\n      hour12: !use24Hour,\n      timeZone: timezone,\n    });\n  } catch {\n    // Fallback if timezone is invalid\n    return date.toLocaleTimeString('en-US', {\n      hour: '2-digit',\n      minute: '2-digit',\n      second: showSeconds ? '2-digit' : undefined,\n      hour12: !use24Hour,\n    });\n  }\n}\n\nexport function formatDate(\n  unixTimestamp: number,\n  format: 'short' | 'medium' | 'long' | 'weekday' | 'dayMonth' = 'medium',\n  timezone?: string\n): string {\n  const date = new Date(unixTimestamp * 1000);\n  const tzOptions = timezone ? { timeZone: timezone } : {};\n\n  try {\n    switch (format) {\n      case 'short':\n        return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', ...tzOptions });\n      case 'long':\n        return date.toLocaleDateString('en-US', {\n          weekday: 'long',\n          month: 'long',\n          day: 'numeric',\n          year: 'numeric',\n          ...tzOptions,\n        });\n      case 'weekday':\n        return date.toLocaleDateString('en-US', { weekday: 'short', ...tzOptions });\n      case 'dayMonth':\n        return date.toLocaleDateString('en-US', { day: 'numeric', month: 'short', ...tzOptions });\n      case 'medium':\n      default:\n        return date.toLocaleDateString('en-US', {\n          weekday: 'short',\n          month: 'short',\n          day: 'numeric',\n          ...tzOptions,\n        });\n    }\n  } catch {\n    return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });\n  }\n}\n\nexport function isDateToday(unixTimestamp: number): boolean {\n  const date = new Date(unixTimestamp * 1000);\n  const today = new Date();\n  return (\n    date.getDate() === today.getDate() &&\n    date.getMonth() === today.getMonth() &&\n    date.getFullYear() === today.getFullYear()\n  );\n}\n\nexport function isDaytime(unixTimestamp: number, sunrise: number, sunset: number): boolean {\n  return unixTimestamp >= sunrise && unixTimestamp <= sunset;\n}\n\nexport function formatDuration(seconds: number): string {\n  const h = Math.floor(seconds / 3600);\n  const m = Math.floor((seconds % 3600) / 60);\n  return `${h}h ${m}m`;\n}\n\n// ---------------------------------------------------------------------------\n// AQI Label\n// ---------------------------------------------------------------------------\nexport interface AQILabelMeta {\n  label: string;\n  description: string;\n  colorClass: string;\n  bgColorClass: string;\n  textColorClass: string;\n  numericRange: string;\n  healthAdvice: string;\n}\n\nexport function getAQILabel(aqi: number): AQILabelMeta {\n  if (aqi <= 50) {\n    return {\n      label: 'Good',\n      description: 'Air quality is satisfactory',\n      colorClass: 'text-emerald-500',\n      bgColorClass: 'bg-emerald-500',\n      textColorClass: 'text-emerald-700',\n      numericRange: '0-50',\n      healthAdvice: 'Enjoy outdoor activities.',\n    };\n  }\n  if (aqi <= 100) {\n    return {\n      label: 'Fair',\n      description: 'Acceptable air quality',\n      colorClass: 'text-yellow-500',\n      bgColorClass: 'bg-yellow-400',\n      textColorClass: 'text-yellow-700',\n      numericRange: '51-100',\n      healthAdvice: 'Sensitive individuals should limit prolonged outdoor exposure.',\n    };\n  }\n  if (aqi <= 150) {\n    return {\n      label: 'Moderate',\n      description: 'Unhealthy for sensitive groups',\n      colorClass: 'text-orange-500',\n      bgColorClass: 'bg-orange-400',\n      textColorClass: 'text-orange-700',\n      numericRange: '101-150',\n      healthAdvice: 'Reduce prolonged outdoor activity if you have respiratory conditions.',\n    };\n  }\n  if (aqi <= 200) {\n    return {\n      label: 'Poor',\n      description: 'Unhealthy for general public',\n      colorClass: 'text-red-500',\n      bgColorClass: 'bg-red-500',\n      textColorClass: 'text-red-700',\n      numericRange: '151-200',\n      healthAdvice: 'Everyone should reduce prolonged outdoor exertion.',\n    };\n  }\n  if (aqi <= 300) {\n    return {\n      label: 'Very Poor',\n      description: 'Very unhealthy',\n      colorClass: 'text-purple-600',\n      bgColorClass: 'bg-purple-600',\n      textColorClass: 'text-purple-800',\n      numericRange: '201-300',\n      healthAdvice: 'Avoid all outdoor exertion.',\n    };\n  }\n  return {\n    label: 'Hazardous',\n    description: 'Emergency health conditions',\n    colorClass: 'text-rose-800',\n    bgColorClass: 'bg-rose-800',\n    textColorClass: 'text-rose-900',\n    numericRange: '301+',\n    healthAdvice: 'Stay indoors. Keep windows and doors closed.',\n  };\n}\n\nexport function getAQICategoryLabel(category: AQICategory): AQILabelMeta {\n  const map: Record<AQICategory, number> = {\n    Good: 25,\n    Fair: 75,\n    Moderate: 125,\n    Poor: 175,\n    'Very Poor': 250,\n    Hazardous: 350,\n  };\n  return getAQILabel(map[category]);\n}\n\n// ---------------------------------------------------------------------------\n// UV Index Label\n// ---------------------------------------------------------------------------\nexport interface UVIndexMeta {\n  label: string;\n  description: string;\n  colorClass: string;\n  bgColorClass: string;\n  spfRecommendation: string;\n  exposureTime: string;\n  level: 'low' | 'moderate' | 'high' | 'very-high' | 'extreme';\n}\n\nexport function getUVIndexLabel(uvIndex: number): UVIndexMeta {\n  const rounded = Math.round(uvIndex);\n\n  if (rounded <= 2) {\n    return {\n      label: 'Low',\n      description: `UV Index: ${rounded}`,\n      colorClass: 'text-emerald-500',\n      bgColorClass: 'bg-emerald-500',\n      spfRecommendation: 'No protection needed',\n      exposureTime: 'No restrictions',\n      level: 'low',\n    };\n  }\n  if (rounded <= 5) {\n    return {\n      label: 'Moderate',\n      description: `UV Index: ${rounded}`,\n      colorClass: 'text-yellow-500',\n      bgColorClass: 'bg-yellow-400',\n      spfRecommendation: 'SPF 15+ recommended',\n      exposureTime: 'Safe up to 30 minutes without protection',\n      level: 'moderate',\n    };\n  }\n  if (rounded <= 7) {\n    return {\n      label: 'High',\n      description: `UV Index: ${rounded}`,\n      colorClass: 'text-orange-500',\n      bgColorClass: 'bg-orange-400',\n      spfRecommendation: 'SPF 30+ required',\n      exposureTime: 'Limit to 20 minutes without protection',\n      level: 'high',\n    };\n  }\n  if (rounded <= 10) {\n    return {\n      label: 'Very High',\n      description: `UV Index: ${rounded}`,\n      colorClass: 'text-red-500',\n      bgColorClass: 'bg-red-500',\n      spfRecommendation: 'SPF 50+ essential',\n      exposureTime: 'Avoid midday sun. Seek shade.',\n      level: 'very-high',\n    };\n  }\n  return {\n    label: 'Extreme',\n    description: `UV Index: ${rounded}`,\n    colorClass: 'text-violet-600',\n    bgColorClass: 'bg-violet-600',\n    spfRecommendation: 'SPF 50+ and full coverage required',\n    exposureTime: 'Stay indoors during peak hours (10am-4pm)',\n    level: 'extreme',\n  };\n}\n\n// ---------------------------------------------------------------------------\n// Temperature unit conversion\n// ---------------------------------------------------------------------------\nexport function convertTemperature(\n  celsius: number,\n  unit: TemperatureUnit\n): number {\n  if (unit === 'fahrenheit') {\n    return parseFloat(((celsius * 9) / 5 + 32).toFixed(1));\n  }\n  return parseFloat(celsius.toFixed(1));\n}\n\nexport function formatTemperature(celsius: number, unit: TemperatureUnit): string {\n  const converted = convertTemperature(celsius, unit);\n  const symbol = unit === 'fahrenheit' ? '°F' : '°C';\n  return `${converted}${symbol}`;\n}\n\n// ---------------------------------------------------------------------------\n// Wind speed conversion\n// ---------------------------------------------------------------------------\nexport function convertWindSpeed(metersPerSecond: number, unit: WindSpeedUnit): number {\n  switch (unit) {\n    case 'mph':\n      return parseFloat((metersPerSecond * 2.23694).toFixed(1));\n    case 'kph':\n      return parseFloat((metersPerSecond * 3.6).toFixed(1));\n    case 'knots':\n      return parseFloat((metersPerSecond * 1.94384).toFixed(1));\n    case 'ms':\n    default:\n      return parseFloat(metersPerSecond.toFixed(1));\n  }\n}\n\nexport function formatWindSpeed(metersPerSecond: number, unit: WindSpeedUnit): string {\n  const value = convertWindSpeed(metersPerSecond, unit);\n  const labels: Record<WindSpeedUnit, string> = {\n    mph: 'mph',\n    kph: 'km/h',\n    ms: 'm/s',\n    knots: 'kn',\n  };\n  return `${value} ${labels[unit]}`;\n}\n\n// ---------------------------------------------------------------------------\n// Pressure conversion\n// ---------------------------------------------------------------------------\nexport function convertPressure(hPa: number, unit: PressureUnit): number {\n  switch (unit) {\n    case 'inHg':\n      return parseFloat((hPa * 0.02953).toFixed(2));\n    case 'mb':\n      return parseFloat(hPa.toFixed(1)); // mb === hPa\n    case 'hPa':\n    default:\n      return parseFloat(hPa.toFixed(1));\n  }\n}\n\nexport function formatPressure(hPa: number, unit: PressureUnit): string {\n  return `${convertPressure(hPa, unit)} ${unit}`;\n}\n\n// ---------------------------------------------------------------------------\n// Moon phase utilities\n// ---------------------------------------------------------------------------\nexport function getMoonPhaseIcon(phase: number): string {\n  // phase is 0-1, where 0/1 = new moon, 0.5 = full moon\n  const index = Math.floor(phase * 8) % 8;\n  const icons = ['🌑', '🌒', '🌓', '🌔', '🌕', '🌖', '🌗', '🌘'];\n  return icons[index];\n}\n\nexport function getMoonPhaseName(phase: number): MoonPhaseName {\n  const index = Math.floor(phase * 8) % 8;\n  const names: MoonPhaseName[] = [\n    'New Moon',\n    'Waxing Crescent',\n    'First Quarter',\n    'Waxing Gibbous',\n    'Full Moon',\n    'Waning Gibbous',\n    'Last Quarter',\n    'Waning Crescent',\n  ];\n  return names[index];\n}\n\n// ---------------------------------------------------------------------------\n// Visibility\n// ---------------------------------------------------------------------------\nexport function getVisibilityLabel(km: number): { label: string; description: string } {\n  if (km >= 20) return { label: 'Excellent', description: 'Crystal clear visibility' };\n  if (km >= 10) return { label: 'Good', description: 'Good visibility conditions' };\n  if (km >= 5) return { label: 'Moderate', description: 'Some haze or light fog' };\n  if (km >= 2) return { label: 'Poor', description: 'Significant haze or fog' };\n  return { label: 'Very Poor', description: 'Dense fog, extremely limited visibility' };\n}\n\n// ---------------------------------------------------------------------------\n// Humidity comfort level\n// ---------------------------------------------------------------------------\nexport function getHumidityLabel(percent: number): { label: string; description: string; colorClass: string } {\n  if (percent < 30) {\n    return { label: 'Dry', description: 'Very dry conditions', colorClass: 'text-amber-500' };\n  }\n  if (percent < 50) {\n    return { label: 'Comfortable', description: 'Ideal humidity range', colorClass: 'text-emerald-500' };\n  }\n  if (percent < 65) {\n    return { label: 'Moderate', description: 'Slightly humid', colorClass: 'text-yellow-500' };\n  }\n  if (percent < 80) {\n    return { label: 'Humid', description: 'Noticeably humid', colorClass: 'text-orange-500' };\n  }\n  return { label: 'Very Humid', description: 'Oppressively humid', colorClass: 'text-red-500' };\n}\n\n// ---------------------------------------------------------------------------\n// Precipitation intensity\n// ---------------------------------------------------------------------------\nexport function getPrecipIntensityLabel(mmPerHour: number): string {\n  if (mmPerHour === 0) return 'None';\n  if (mmPerHour < 0.5) return 'Very Light';\n  if (mmPerHour < 2) return 'Light';\n  if (mmPerHour < 5) return 'Moderate';\n  if (mmPerHour < 10) return 'Heavy';\n  return 